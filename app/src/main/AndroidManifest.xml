<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="duan.it.thanh.duanmobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Music.ListMusicActivity"
            android:label="@string/title_activity_list_music"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Music.ViewMusicActivity"
            android:label="@string/title_activity_view_music"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Media.TypeMediaActivity"
            android:label="@string/title_activity_type_media"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Media.ListMediaActivity"
            android:label="@string/title_activity_list_media"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>